exit
if @student.active == false
if @student.active == false {@student.active = true} 
    end
      @student.active = false
    elsif @student.active == true
    @student.active = true
if @student.active == false
@student.active
@student.active = true
@student.active
@student
exit
@student = Student.find(params[:id])
params
exit
@student
exit
@student
  end
    expect(@student.active).to eq(true)
    @student.reload
    visit activate_student_path(@student)
it "Should mark an inactive student as active" do
visit activate_student_path(@student)
exit
@student
exit
params
@student = Student.all.find_by(params[:id])
exit
@student.active
@student
@student = Student.all.find_by(params[:id])
exit
@students
exit
@students
